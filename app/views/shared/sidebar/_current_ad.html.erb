<div class="ad_box">
  <ul class="cube_crossfade">
    <% ads.each do |adv| %>
    <li><%= link_to(image_tag(adv.ad_image(:reg), :alt => adv.title), "#{adv.link_url}") %></li>
    <% end %>
  </ul>
</div>
